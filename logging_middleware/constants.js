const STACK = ["backend", "frontend"];

const LEVEL = ["debug", "info", "warn", "error", "fatal"];

const PACKAGE = [
  "cache",
  "controller",
  "cron_job",
  "db",
  "domain",
  "handler"
];

module.exports = { STACK, LEVEL, PACKAGE };